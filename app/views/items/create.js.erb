item_saved ="<%= @item_saved %>"

if (item_saved == 1){
	variable = "<%= raw escape_javascript(render partial: 'shared/displayitems') %>";
	$("#postDisplay").html(variable);
	document.getElementById("item_form").reset();
} else {
	variable = "<%= raw escape_javascript(render partial: 'shared/error_messagesitems') %>";
	$("#errorMessage").html(variable);
}